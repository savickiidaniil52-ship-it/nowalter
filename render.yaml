services:
  - type: web
    name: nowalter
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
    disk:
      name: nowalter-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
